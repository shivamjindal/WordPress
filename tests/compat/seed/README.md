# Compatibility seed data

Use `tools/compat-capture/seed-db.sh` to initialize a repeatable database state
before running baseline capture.

The default seed file is `wordpress_seed.sql` and can be customized per
environment by replacing its contents with deterministic fixtures.
